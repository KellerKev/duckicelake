# Local backends (pixi-managed, never checked in)
.pixi/
.pgdata/
.pgsock/
.miniodata/
.data/

# Build / test artefacts
.pytest_cache/
__pycache__/
*.pyc
*.egg-info/

# Editor / tool state
.claude/
.python-version
.DS_Store

# Secrets — never commit env files
.env
.env.*
!.env.example

# Local config file (may carry secrets) — commit only the example
duckicelake.toml

# SMCP interface config (secrets) + RS256/TLS keys — never commit
smcp.toml
keys/
